# Since the ".env" file is gitignored, copy this file to `.env` and fill in values.

# Prisma — local PostgreSQL via Docker (see docker-compose.yml)
DATABASE_URL="postgresql://postgres:password@localhost:5432/pkbl"

# Optional: skip env validation during Docker builds or CI
# SKIP_ENV_VALIDATION=true

# Docker: pnpm db:up  (requires Docker Desktop running)
